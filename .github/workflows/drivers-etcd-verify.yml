# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/etcd/.github/workflows/verify.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Verify Etcd Driver

on:
  push:
    branches:
      - 'master'
      - 'actions'
    paths:
      - 'drivers/etcd/**'
  pull_request:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      component: drivers/etcd
      package: github.com/atomix/atomix/drivers/etcd/v3/...

  lint:
    uses: ./.github/workflows/lint.yml
    with:
      component: drivers/etcd
