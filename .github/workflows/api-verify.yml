# Code generated by build-workflows script. DO NOT EDIT.
# source: api/.github/workflows/verify.yml

name: Verify API

on:
  push:
    branches:
      - 'master'
    tags:
      - 'api/v*'
    paths:
      - 'api/**'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.19
      - name: Cache Go modules
        uses: actions/cache@v1
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-
      - name: Run tests
        working-directory: api
        run: make test