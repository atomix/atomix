# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/redis/.github/workflows/test.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test Redis Driver

on:
  push:
    branches:
      - 'master'
    tags:
      - 'drivers/redis/v*'
    paths:
      - 'drivers/redis/**'
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    name: Check code quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Go linter
        uses: ./.github/actions/go/lint
        with:
          component: drivers/redis

  test:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Test atomix.io/redis@v8
        uses: ./.github/actions/go/test
        with:
          component: drivers/redis
          package: github.com/atomix/atomix/drivers/redis/v8/driver/...

      - name: Test atomix.io/redis@v9
        uses: ./.github/actions/go/test
        with:
          component: drivers/redis
          package: github.com/atomix/atomix/drivers/redis/v9/driver/...
