# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/raft/.github/workflows/verify.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Verify Raft Driver

on:
  push:
    branches:
      - 'master'
    tags:
      - 'drivers/raft/v*'
    paths:
      - 'drivers/raft/**'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Check plugin compatibility
        uses: ./.github/actions/driver/check-compatibility
        with:
          path: drivers/raft

      - name: Run tests
        uses: ./.github/actions/go/test
        with:
          path: drivers/raft
          package: github.com/atomix/atomix/drivers/raft/driver/...
