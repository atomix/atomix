# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/pod-memory/.github/workflows/test.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test Pod Memory Driver

on:
  push:
    branches:
      - 'master'
    tags:
      - 'drivers/pod-memory/v*'
    paths:
      - 'drivers/pod-memory/**'
  pull_request:

jobs:
  lint:
    name: Check code quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Go linter
        uses: ./.github/actions/go/lint
        with:
          component: drivers/pod-memory

  test:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Test atomix.io/pod-memory@v1
        uses: ./.github/actions/go/test
        with:
          component: drivers/pod-memory
          package: github.com/atomix/atomix/drivers/pod-memory/v1/driver/...
