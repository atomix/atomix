# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/pod-memory/.github/workflows/test.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test pod-memory driver

on:
  push:
    branches:
      - 'master'
      - 'actions'
    paths:
      - 'controller/**'
      - 'sidecar/**'
      - 'drivers/pod-memory/**'
      - 'stores/pod-memory/**'
  pull_request:
  workflow_dispatch:

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Helm
        uses: ./.github/actions/setup-helm

      - name: Create kind cluster
        uses: helm/kind-action@v1.4.0
        with:
          cluster_name: test

      - name: Build atomix/sidecar
        uses: docker/build-push-action@v3
        with:
          context: .
          file: drivers/pod-memory/test/Dockerfile
          tags: atomix/sidecar
          load: true
          push: false

      - name: Build atomix/controller
        uses: docker/build-push-action@v3
        with:
          context: controller
          file: controller/build/controller/Dockerfile
          tags: atomix/controller
          load: true
          push: false

      - name: Build atomix/controller-init
        uses: docker/build-push-action@v3
        with:
          context: controller
          file: controller/build/controller-init/Dockerfile
          tags: atomix/controller-init
          load: true
          push: false

      - name: Build atomix/pod-memory-controller
        uses: docker/build-push-action@v3
        with:
          context: stores/pod-memory
          file: stores/pod-memory/build/controller/Dockerfile
          tags: atomix/pod-memory-controller
          load: true
          push: false

      - name: Load images
        run: kind load docker-image atomix/sidecar atomix/controller atomix/controller-init atomix/pod-memory-controller --name test

      - name: Install core controller
        run: helm install -n kube-system atomix-controller ./controller/chart --wait

      - name: Install store controller
        run: helm install -n kube-system atomix-pod-memory-controller ./store/pod-memory/chart --wait

      - name: Run tests
        run: ct install --charts store/pod-memory/test
