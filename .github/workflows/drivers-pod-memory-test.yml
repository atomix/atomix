# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/pod-memory/.github/workflows/test.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test pod-memory driver

on:
  push:
    branches:
      - 'master'
      - 'actions'
    paths:
      - 'controller/**'
      - 'sidecar/**'
      - 'drivers/pod-memory/**'
      - 'stores/pod-memory/**'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Go
        uses: ./.github/actions/setup-go

      - name: Setup Helm
        uses: ./.github/actions/setup-helm

      - name: Create kind cluster
        uses: helm/kind-action@v1.4.0
        with:
          cluster_name: kind

      - name: Build sidecar
        run: docker build . -t atomix/sidecar -f drivers/pod-memory/test/Dockerfile && kind load docker-image atomix/sidecar

      - name: Build controller
        run: make kind
        working-directory: controller

      - name: Build sidecar controller
        run: make controller-kind
        working-directory: sidecar

      - name: Build store
        run: make kind
        working-directory: stores/pod-memory

      - name: Install core controller
        run: helm install -n kube-system atomix-controller ./controller/chart --wait

      - name: Install sidecar controller
        run: helm install -n kube-system atomix-sidecar-controller ./sidecar/chart --wait

      - name: Install store controller
        run: helm install -n kube-system atomix-pod-memory-controller ./stores/pod-memory/chart --wait

      - name: Run tests
        run: ct install --charts ./drivers/pod-memory/test
