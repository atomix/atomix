# Code generated by build-workflows script. DO NOT EDIT.
# source: charts/atomix/.github/workflows/verify.yml

name: Verify Atomix Helm Chart

on:
  push:
    branches:
      - 'master'
    paths:
      - 'charts/atomix/**'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Verify
        uses: ./.github/actions/chart/verify
        with:
          working-directory: charts/atomix
