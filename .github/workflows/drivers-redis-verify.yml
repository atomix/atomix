# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/redis/.github/workflows/verify.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Verify Redis Driver v8

on:
  push:
    branches:
      - 'master'
    paths:
      - 'drivers/redis/**'
  pull_request:
    paths:
      - 'drivers/redis/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-v8:
    uses: ./.github/workflows/test.yml
    with:
      component: drivers/redis
      package: github.com/atomix/atomix/drivers/redis/v8/...

  lint-v8:
    uses: ./.github/workflows/lint.yml
    with:
      component: drivers/redis

  test-v9:
    uses: ./.github/workflows/test.yml
    with:
      component: drivers/redis
      package: github.com/atomix/atomix/drivers/redis/v9/...

  lint-v9:
    uses: ./.github/workflows/lint.yml
    with:
      component: drivers/redis