# Code generated by build-workflows script. DO NOT EDIT.
# source: controller/.github/workflows/test.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test Controller

on:
  push:
    branches:
      - 'master'
    paths:
      - 'controller/**'
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    name: Check code quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Go linter
        uses: ./.github/actions/go/lint
        with:
          component: controller

  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Run Go tests
        uses: ./.github/actions/go/test
        with:
          component: controller

  build-controller:
    name: Build controller image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Build Docker image
        uses: ./.github/actions/docker/build
        with:
          component: controller
          file: build/controller/Dockerfile

  build-controller-init:
    name: Build controller-init image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Build Docker image
        uses: ./.github/actions/docker/build
        with:
          component: controller
          file: build/controller-init/Dockerfile
