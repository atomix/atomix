# Code generated by build-workflows script. DO NOT EDIT.
# source: runtime/.github/workflows/test.yml

name: Test Runtime

on:
  push:
    branches:
      - 'master'
    tags:
      - 'runtime/v*'
    paths:
      - 'runtime/**'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run tests
        uses: ./.github/actions/go/test
        with:
          path: runtime
