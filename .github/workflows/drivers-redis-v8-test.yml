# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/redis/v8/.github/workflows/test.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test Redis v8 Driver

on:
  push:
    branches:
      - 'master'
    tags:
      - 'drivers/redis/v8/v*'
    paths:
      - 'drivers/redis/v8/**'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Check plugin compatibility
        uses: ./.github/actions/driver/check-compatibility
        with:
          path: drivers/redis/v8

      - name: Run tests
        uses: ./.github/actions/go/test
        with:
          path: drivers/redis/v8
          package: github.com/atomix/atomix/drivers/redis/v8/driver/...
