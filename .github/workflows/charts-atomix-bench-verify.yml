# Code generated by build-workflows script. DO NOT EDIT.
# source: charts/atomix-bench/.github/workflows/verify.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Verify Atomix Benchmark Helm Chart

on:
  push:
    branches:
      - 'master'
    paths:
      - 'charts/atomix-bench/**'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Verify
        uses: ./.github/actions/chart/verify
        with:
          working-directory: charts/atomix-bench
