# Code generated by build-workflows script. DO NOT EDIT.
# source: stores/raft/.github/workflows/verify.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Verify Raft Store

on:
  push:
    branches:
      - 'master'
      - 'actions'
    paths:
      - 'stores/raft/**'
  pull_request:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/common/test.yml@${{ github.sha }}
    with:
      component: stores/raft

  lint:
    uses: ./.github/common/lint.yml@${{ github.sha }}
    with:
      component: stores/raft
      chart: chart
