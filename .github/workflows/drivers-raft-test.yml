# Code generated by build-workflows script. DO NOT EDIT.
# source: drivers/raft/.github/workflows/test.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test raft driver

on:
  push:
    branches:
      - 'master'
      - 'actions'
    paths:
      - 'controller/**'
      - 'sidecar/**'
      - 'drivers/raft/**'
      - 'stores/raft/**'
      - 'test/**'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-primitives:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Go
        uses: ./.github/actions/setup-go

      - name: Setup Helm
        uses: ./.github/actions/setup-helm

      - name: Create kind cluster
        uses: helm/kind-action@v1.4.0
        with:
          cluster_name: kind

      - name: Build CLI
        run: make
        working-directory: cli

      - name: Build sidecar
        run: docker build . -t atomix/sidecar -f drivers/raft/test/Dockerfile && kind load docker-image atomix/sidecar

      - name: Build controller
        run: make kind
        working-directory: controller

      - name: Build sidecar controller
        run: make controller-kind
        working-directory: sidecar

      - name: Build store
        run: make kind
        working-directory: stores/raft

      - name: Build tests
        run: make kind
        working-directory: test

      - name: Install core controller
        run: helm install -n kube-system atomix-controller ./controller/chart --wait

      - name: Install sidecar controller
        run: helm install -n kube-system atomix-sidecar-controller ./sidecar/chart --wait

      - name: Install store controller
        run: helm install -n kube-system atomix-raft-controller ./stores/raft/chart --wait

      - name: Setup tests
        run: kubectl create -f drivers/pod-memory/test/setup.yaml

      - name: Install tests
        run: helm install test ./test/chart -f drivers/pod-memory/test/suite.yaml --wait

      - name: Run tests
        run: helm test test
