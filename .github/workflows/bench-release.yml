# Code generated by build-workflows script. DO NOT EDIT.
# source: bench/.github/workflows/release.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Release Benchmarking Tool

on:
  push:
    tags:
      - 'bench/v*'

jobs:
  build-and-release:
    name: Build and release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - id: validate-version
        name: Parse and validate version
        uses: ./.github/actions/version/validate
        with:
          tag: ${{ github.ref_name }}

      - name: Build Docker image
        uses: ./.github/actions/docker/build
        with:
          path: bench
          dockerfile: build/Dockerfile
          repository: atomix/bench

      - name: Publish Docker image
        uses: ./.github/actions/docker/publish
        with:
          repository: atomix/bench
          tag: v${{ steps.validate-version.outputs.version }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Publish Helm chart
        uses: ./.github/actions/helm/publish
        with:
          path: bench/chart
          version: ${{ steps.validate-version.outputs.version }}
          token: ${{ secrets.GH_TOKEN }}
