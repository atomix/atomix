# Code generated by build-workflows script. DO NOT EDIT.
# source: bench/.github/workflows/release.yml

name: Release Benchmarks

on:
  push:
    tags:
      - 'bench/v*'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Release
        uses: ./.github/actions/module/release.yml
        with:
          working-directory: bench