# Code generated by build-workflows script. DO NOT EDIT.
# source: charts/atomix-bench/.github/workflows/publish.yml

# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Publish Benchmark Helm Chart

on:
  push:
    branches:
      - 'master'
    paths:
      - 'charts/atomix-bench/**'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Publish
        uses: ./.github/actions/chart/publish
        with:
          working-directory: charts/atomix-bench
          github-token: ${{ secrets.GH_TOKEN }}
