# Code generated by build-workflows script. DO NOT EDIT.
# source: protocols/rsm/.github/workflows/verify.yml

name: Verify RSM Protocol

on:
  push:
    branches:
      - 'master'
    tags:
      - 'protocols/rsm/v*'
    paths:
      - 'protocols/rsm/**'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run tests
        uses: ./.github/actions/go/test
        with:
          path: protocols/rsm
