name: Build Module
inputs:
  working-directory:
    description: 'The working directory'
    required: true
    default: ''
runs:
  using: composite
  steps:
    - name: Build Docker image
      run: make build
      shell: bash
      working-directory: ${{ inputs.working-directory }}
