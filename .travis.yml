language: java
jdk:
  - oraclejdk8
  - openjdk8

notifications:
  email: false

branches:
  only:
    - master

install:
  - ./mvnw clean install -DskipTests=true -Dmaven.javadoc.skip=true -Dmaven.test.skip=true -B -V
script:
  - ./mvnw verify -Droot.logging.level=INFO

after_success:
  - ./mvnw jacoco:report coveralls:report -Droot.logging.level=INFO
  - bin/ci-push-javadoc.sh
  - bin/ci-deploy-snapshot.sh