# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

apiVersion: atomix.io/v3beta4
kind: DataStore
metadata:
  name: {{ .Release.Name }}
spec:
  driver:
    name: {{ .Values.test.driver }}
    apiVersion: {{ .Values.test.apiVersion }}
  config:
    Addr: {{ .Release.Name }}-redis:6379
