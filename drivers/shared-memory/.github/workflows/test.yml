# SPDX-FileCopyrightText: 2023-present Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0

name: Test shared-memory driver

on:
  push:
    branches:
      - 'master'
      - 'actions'
    paths:
      - 'controller/**'
      - 'sidecar/**'
      - 'drivers/shared-memory/**'
      - 'stores/shared-memory/**'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Helm
        uses: ./.github/actions/setup-helm

      - name: Create kind cluster
        uses: helm/kind-action@v1.4.0
        with:
          cluster_name: test

      - name: Build atomix/controller
        uses: docker/build-push-action@v3
        with:
          context: controller
          file: controller/build/controller/Dockerfile
          tags: atomix/controller
          load: true
          push: false

      - name: Build atomix/controller-init
        uses: docker/build-push-action@v3
        with:
          context: controller
          file: controller/build/controller-init/Dockerfile
          tags: atomix/controller-init
          load: true
          push: false

      - name: Build atomix/sidecar
        uses: docker/build-push-action@v3
        with:
          context: .
          file: drivers/shared-memory/test/Dockerfile
          tags: atomix/sidecar
          load: true
          push: false

      - name: Build atomix/sidecar-controller
        uses: docker/build-push-action@v3
        with:
          context: sidecar
          file: sidecar/build/controller/Dockerfile
          tags: atomix/sidecar-controller
          load: true
          push: false

      - name: Build atomix/shared-memory-controller
        uses: docker/build-push-action@v3
        with:
          context: stores/shared-memory
          file: stores/shared-memory/build/controller/Dockerfile
          tags: atomix/shared-memory-controller
          load: true
          push: false

      - name: Build atomix/shared-memory-node
        uses: docker/build-push-action@v3
        with:
          context: stores/shared-memory
          file: stores/shared-memory/build/node/Dockerfile
          tags: atomix/shared-memory-node
          load: true
          push: false

      - name: Load images
        run: kind load docker-image atomix/controller atomix/controller-init atomix/sidecar atomix/sidecar-controller atomix/shared-memory-controller atomix/shared-memory-node --name test

      - name: Install core controller
        run: helm install -n kube-system atomix-controller ./controller/chart --wait

      - name: Install sidecar controller
        run: helm install -n kube-system atomix-sidecar-controller ./sidecar/chart --wait

      - name: Install store controller
        run: helm install -n kube-system atomix-shared-memory-controller ./stores/shared-memory/chart --wait

      - name: Run tests
        run: ct install --charts ./store/shared-memory/test
